Development: `pnpm dev`, `pnpm storybook`, `pnpm build-storybook`.
Validation: `pnpm format:check && pnpm lint && pnpm test && pnpm build`.
Focused checks: `pnpm test -- src/components/__tests__/VueModalDialog.test.ts`, `pnpm test -- -t "emits closed"`.
Formatting/linting: `pnpm format`, `pnpm format:check`, `pnpm lint`.
Build: `pnpm build`.